<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | DeadlineDefender</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <!-- Bootstrap Icons (optional) -->
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
  />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  <!-- Navigation bar -->
  <nav class="navbar navbar-expand-lg navbar-dark dd-navbar">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">DeadlineDefender</a>

      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="/assignments">Dashboard</a>
          </li>

          <% if (user) { %>
            <!-- Logged-in: show Add Task + Logout only -->
             <li class="nav-item">
              <a class="nav-link" href="/assignments/add">Add Task</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/auth/logout">Logout</a>
            </li>
            <% } else { %>
              <!-- Logged-out: single Login link -->
               <li class="nav-item">
                <a class="nav-link" href="/login">Login</a>
              </li>
              <% } %>

        </ul>
      </div>
    </div>
  </nav>

  <!-- Main page content -->
  <main class="container my-4 dd-main">
